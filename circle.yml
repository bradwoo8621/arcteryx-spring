machine: 
  java:
    version: oraclejdk8
test:
  post:
    - mvn jacoco:report
    - curl http://www.jpm4j.org/install/script > jpmInstall.sh
    - sudo sh jpmInstall.sh
    - sudo jpm install com.codacy:codacy-coverage-reporter:assembly
    - codacy-coverage-reporter -l Java -t 37a14bdab2214e2c9674672f46d10a5d -r target/site/jacoco/jacoco.xml